export default class TouchPoint {

  // construction
  constructor(point) {
    this.point = point;
    this.groups = [];
    this.isPaired = false;
  }
  //----------------------------------------------------------------------------
}
